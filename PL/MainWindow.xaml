<Window x:Class="PL.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="/לשרה.png"/>
        </Grid.Background>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button x:Name="Managerview" Content="Manager view" Click="Manager_Click" Grid.Column="0" Background="{x:Null}"/>
        <Button x:Name="Neworderdisplay" Content="New order display" Click="Neworder_Click" Grid.Column="1" Background="{x:Null}"/>
        <Button x:Name="Ordertrackingview" Content="Order tracking view"  Click="OrderTracking_Click" Grid.Column="2" Background="{x:Null}"/>
        <Button x:Name="registereduser" Content="registered user" Click="registeredUser_Click" Grid.Column="3" Background="{x:Null}"/>
    </Grid>
</Window>
